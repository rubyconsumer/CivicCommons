<% if recent_items.size > 0 %>
  <div class="mod">
    <h3>Recent Activity</h3>
    <div class='mod-content'>
      <%= render :partial => 'shared/recent_item', :collection => recent_items, :as => :recent_item %>
    </div>
  </div>
<% end %>
