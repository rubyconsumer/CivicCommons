<%= remotipart_response do %>
  $.colorbox({
    html: "<%= escape_javascript(
            raw(
              render( :partial => 'sign_modal' )
            )
    ) %>",
    onComplete: function(){
      $.colorbox.resize();
    }
  });
<% end %>
