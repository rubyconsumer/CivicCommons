<h1>Listing admin_surveys</h1>

<table>
  <tr>
    <th>Surveyable</th>
    <th>Surveyable type</th>
    <th>Title</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @surveys.each do |survey| %>
  <tr>
    <td><%= survey.surveyable_id %></td>
    <td><%= survey.surveyable_type %></td>
    <td><%= survey.title %></td>
    <td><%= survey.description %></td>
    <td><%= link_to 'Show', admin_survey_path(survey) %></td>
    <td><%= link_to 'Edit', edit_admin_survey_path(survey) %></td>
    <td><%= link_to 'Destroy', admin_survey_path(survey), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Survey', new_admin_survey_path %>
