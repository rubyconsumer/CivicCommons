<%= form_for(@admin_curated_feed) do |f| %>
  <% if @admin_curated_feed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_curated_feed.errors.count, "error") %> prohibited this admin_curated_feed from being saved:</h2>

      <ul>
      <% @admin_curated_feed.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
