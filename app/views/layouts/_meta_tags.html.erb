<%
  title = "The Civic Commons"
  description = "The Civic Commons is a new way to bring communities together with conversation and emerging technology. We’re focused on building conversations and connections that have the power to become informed, productive collective civic action."
  image = "/images/cc_podcast_small.jpg"

  if @meta_info.present?
          title = Sanitize.clean(@meta_info[:page_title], :remove_contents => ['style','script'])       if @meta_info[:page_title].present?
    description = Sanitize.clean(@meta_info[:meta_description], :remove_contents => ['style','script']) if @meta_info[:meta_description].present?
          image = Sanitize.clean(@meta_info[:image_url], :remove_contents => ['style','script'])        if @meta_info[:image_url].present?
  end

%>

<meta name="title" content="<%=h title %>" />
<meta name="description" content="<%=h description %>" />
<% if not image.nil? %>
<link rel="image_src" href="<%= image %>" />
<% end %>
<meta name="keywords" content="<%= meta_keywords %>" />
<meta name="medium" content="blog" />


