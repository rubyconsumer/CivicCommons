<div class="feature-mast">
  <div class="wrapper">
<div class="content-container">
  <%= render :partial => '/articles/featured' %>
</div><!-- /.content-container -->
</div>
</div>

<div class="wrapper">
<div class="content-container">
  <h1>Issues</h1>
  <div class="main-content">
    <h3>Most Recent Issues</h3>
    <table class="data-table">
      <colgroup class="issue-title"></colgroup>
      <colgroup class="contrib-count"></colgroup>
      <colgroup class="convo-count"></colgroup>
      <tbody>
        <% @issues.each do |issue| %>
	<tr>
	  <th scope="row">
            <%= link_to issue.name, issue_path(issue) %>
          </th>
	  <td>
            <%= pluralize(issue.contributions.size, "Contribution") %>
          </td>
	  <td>
            <%= pluralize(issue.conversations.size, "Conversation") %>
          </td>
	</tr>
        <% end %>
      </tbody>
    </table>
    <div class="pagination">
      <%= will_paginate @issues, :inner_window => 3, :previous_label => "Previous", :next_label => "Next" %>
    </div>
  </div><!-- /.main-content -->			
  <div class='aside supplementary'>
    <%= render_recent_items_sidebar %>
    <%= render :partial => '/regions/index' %>
  </div><!-- /.aside -->			
</div><!-- /.content-container -->
</div>
</div>
