%li
  .article.comment
    .meta
      .comment-type.question{:class=>contribution.official ? "" : "unofficial"}
        - "Official" if contribution.official
      %img{:alt=>" [#{contribution.person.name} image] ", :height => 25, :width => 25, :src=>"#{contribution.person.avatar_url(:small)}"}
      %a.rating(href="#" title="Rate this comment")=format_rating(contribution)
    %p
      %a{:href=>person_path(contribution.person), :title=>"#{contribution.person.name}'s Profile"}
        %strong= contribution.person.name
      - if contribution.askee.nil?
        %span.action asked a question.
      - else
        %span.action asked a question to:
        %a.question_recipient(href="#" title="Frank Jackson's Profile")=contribution.askee.name             
    %p.content= contribution.content
    %p
      - if person_signed_in?
        = link_to("Respond", {:action => "new_node_contribution", :contribution_id => contribution.id, :id => contribution.conversation_id, :div_id => "contribution-#{contribution.id}"}, :class => "text button conversation-action", :id => "action-link-#{contribution.id}", :title => "Leave a comment here", 'data-target' => "#action-div-#{contribution.id}", :remote => true)
      - if can_edit?(contribution.owner)
        %a{:href=>"#", :class=>"privileged", :title=>"Delete this comment"} Delete
  %ul
    .question-contribution-action(id="action-div-#{contribution.id}")
    = display_direct_descendant_subset(@contribution, contribution.id)