%ul
  = link_to("Respond", {:action => "new_node_contribution", :contribution_id => @contribution.id, :id => @contribution.conversation_id, :div_id => "contribution-#{@top_level_contribution.id}"}, :class => "text button conversation-action", :id => "action-link-#{@top_level_contribution.id}", :title => "Leave a comment here", 'data-target' => "#action-div-#{@top_level_contribution.id}-respond", :remote => true)
  
  %div{:id => "action-div-#{@top_level_contribution.id}-respond"}

  = display_direct_descendant_subset(@top_level_contribution, @top_level_contribution.id)