/ if params[:preview] is set, then contribution was just submitted for preview, and this form is now the final.
/ otherwise, set this form to submit for preview.
= form_for @contribution, :as => :contribution, :url => "confirm_node_contribution", :remote => true, :html => {'data-type' => 'html'} do |f|
  = f.hidden_field(:id)
  = hidden_field_tag(:div_id, div_id)
  %div.validation-error
  = f.submit("Submit", :class => 'button')
  or
  %a{:href => "##{div_id}-new-#{@contribution.type.to_s.underscore}", :class => "cancel"} Cancel
