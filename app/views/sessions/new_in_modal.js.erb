<%= remotipart_response do %>
  $.colorbox({
    html: "<%= escape_javascript(
            raw(
              render( :partial => 'sessions/new' )
            )
    ) %>",
    onComplete: function(){
      $('#colorbox').find('[placeholder]').addClass('placeholder');
      $.colorbox.resize();
    }
  });
<% end %>
