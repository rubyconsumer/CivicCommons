<%= remotipart_response do %>
  $.colorbox({
    html: "<%= escape_javascript(
            raw(
              render( :partial => 'sessions/new' )
            )
    ) %>",
    onComplete: function(){
      $('#colorbox').find('[placeholder]').placeholder({className: 'placeholder'});
      $.colorbox.resize();
    }
  });
<% end %>
