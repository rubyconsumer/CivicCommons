rvm use --create --install --passenger 1.9.2@civiccommons >/dev/null 2>&1

if [[ -s "./civiccommons.gems" ]]; then
  if ! rvm gemset import civiccommons.gems >/dev/null 2>&1; then
    echo "ERROR: Unable to bootstrap the gems" >&2
  fi
fi
